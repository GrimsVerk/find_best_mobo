# Oracle handoff — 2026-08-20, run 6

Scope commissioned: BL-20, BL-21. Both decided; two decisions appended to
`docs/DESIGN.oracle.md` (OD-19, OD-20), no new requirements, no halts, no
waivers. Written once, per the rule — a correction gets the next-numbered file.

## What needs planning

**Nothing.** Neither decision adds a requirement, so there is nothing for the
steward to cut. Both rulings resolve process hazards whose fixes live in
owner-owned paths; what an oracle-writable path could do — put a durable
prohibition and a durable reading into the design layer the review gate and
every planner reads — is done.

## What the orchestrator must observe from now on

- **Never build `whole-transcript-threshold` slice 1.** OD-19: the slice is
  superseded (pre-clustering, pre-R1000 ratio, its "pending — do not build"
  ceiling uncertainty answered by the owner's 2026-08-19 ruling and OD-13). The
  driver WILL eventually emit `PHASE=ORCHESTRATE SLUG=whole-transcript-threshold`
  once the queue ahead of it drains — that dispatch is expected and is not an
  instruction that overrides the design layer. The dispatched orchestrator
  builds nothing, opens no pull request, reports OD-19, and stops.
- **Never commission planning for R1001, and read its coverage gap as false.**
  OD-20: `coverage.sh` reports `R1001 NOT PLANNED` (exit 1) permanently because
  the script does not read `**Requirements superseded:**`. A coverage exit 1
  whose only gaps are superseded ids means "everything is planned". Do not let
  any plan name R1001 (or any superseded id) in `covers:` to green the report —
  that is the over-claim BL-21 exists to warn about.

## For the owner (surface in the report; act on nothing)

- **One closing edit on `docs/plans/whole-transcript-threshold.md`** — theirs
  alone, any of BL-20's three: `status: merged` (honest only after
  `feat/capped-whole-transcript-path` merges), delete the plan, or cut slice 1
  and R28 from it. Until one lands, OD-19's prohibition is what keeps the
  inevitable dispatch harmless.
- **The `coverage.sh` supersession fix** — owner-owned, and the hole is the
  template's, so it belongs upstream. OD-20 records the recommended shape:
  subtract ids named by column-anchored `**Requirements superseded:**` lines
  and report them as their own excused class, never silent subtraction.

## Do NOT act on

- Editing `docs/plans/whole-transcript-threshold.md`, `.github/scripts/
  coverage.sh`, or `.claude/scripts/deliver-phase.sh` — all owner-owned; both
  decisions say so and route the fixes to the owner and the template.
- **BL-1 and BL-2** — still the only unruled backlog items, still outside the
  commissioned scope. Hand them to the next oracle cycle.

## Ledger state after this run

Every ESC through ESC-23 and every BL through BL-21 is now either ruled
(OD-1..OD-20) or explicitly deferred with its reason (BL-1, BL-2).
`docs/acceptance.md` has no failing-criterion filings; no waiver was needed.

## For the owner's report

Nothing ambiguous in `docs/VISION.md` this run. OD-19 uses the no-vision class
for driver mechanics, consistent with OD-1/OD-2/OD-10/OD-12/OD-18 — the class
doing its job. OD-20 found V12's "Silence has to read as silence" doing real
work one level down, exactly as OD-16 read it for fixture provenance: it is
what turned "subtract the id" into "subtract it and report the retirement as
its own class". That sentence keeps earning its place.
