# Oracle handoff — 2026-08-20, run 1

Scope commissioned: BL-14, BL-15. Both decided; two decisions appended to
`docs/DESIGN.oracle.md` (OD-13, OD-14). No halts. Written once, per the rule —
a correction gets the next-numbered file.

## What needs planning

**1. OD-13 — re-cut the whole-transcript routing plan (R28 + R1008).**

`docs/plans/oracle/capped-whole-transcript-path.md` is merged and **must not be
built as merged**: its slice 1 routes a saturated over-cap video back to
excerpts (`EXCERPTS_OVER_CAP`), its slice 2 asserts no whole block exceeds the
bundle cap, and its slice 3 counts `videos_over_cap` — all of which implement
R1001, which OD-13 superseded. The steward re-cuts it under
`docs/plans/oracle/` citing OD-13, to the clustering design the owner landed
(R5, R28):

- the whole-transcript path is uncapped — a video at or above the 80% ratio is
  sent whole regardless of size;
- a transcript larger than one bundle's token cap is delivered across
  **sequential bundles**, never bounced back to excerpts; the mechanics
  (ordering, how a multi-bundle transcript is marked so the extraction agent
  knows it holds a part) are the plan's to specify;
- the projection reports per-path counts, characters and tokens, plus how many
  whole-path videos exceed one bundle and the bundles each spans (R1008);
- much of the merged plan survives re-use: `submission.py` as the routing home,
  the ratio measured on re-cut post-cap excerpts, characters both sides joined
  identically, the `form` attribute in the bundle XML.

Simplest shape: revise the existing file in place — it lives under
`docs/plans/oracle/`, which is steward-writable and not append-only, and
reusing the slug avoids a second plan naming the same routing. That choice is
yours, not a ruling.

**Sequencing constraint, unchanged from the old plan:**
`docs/plans/oracle/recut-merged-excerpts.md` (OD-4 / R1000) is merged and not
yet built — `merge_overlapping` still concatenates. Build it first: the 80%
ratio the routing reads is only honest once each transcript character is
counted once. One pipeline pull request in flight at a time, as always.

**2. OD-14 — the zero-duration warning (R1009).**

Small, self-contained slice: `src/find_best_mobo/commands/index.py` (the
count-based trigger in `run` and `_warn_zero_duration`), possibly
`src/find_best_mobo/index.py` if the plan wants the Shorts-shape judgment next
to `classify`, plus tests. The requirement fixes behaviour, not mechanism: a
dateless-and-durationless entry is the expected Shorts shape (one summary
line, no banner); a dated-but-durationless entry is the anomaly (banner, ids
named). The test pair — one entry of each shape, asserting the warning fires
for exactly one of them — is part of the requirement. Independent of item 1;
sequence it wherever it fits.

## Do NOT act on

- **`docs/plans/whole-transcript-threshold.md`** — still partly wrong (its
  slice 1 is superseded, its "pending" uncertainty is answered), but
  `docs/plans/` outside `oracle/` is CODEOWNERS-held: revising it is the
  owner's edit, already requested in the merged capped plan's summary. Leave
  it; just never build its slice 1.
- **Building `capped-whole-transcript-path` as merged** — see above. Nothing
  from OD-13 authorises the capped behaviour; the re-cut lands first.
- **BL-1 and BL-2** — the only remaining unruled backlog items, outside this
  run's commissioned scope. Hand them to the next oracle cycle; BL-2 was
  already noted in OD-12 as able to ride the same plan revision when ruled.

## Ledger state after this run

Every ESC through ESC-23 and every BL through BL-15 is now either ruled
(OD-1..OD-14) or explicitly deferred with its reason (BL-1, BL-2 — out of
scope this run). `docs/acceptance.md` has no failing-criterion filings; no
waiver was needed or written.
