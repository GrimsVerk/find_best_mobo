# Oracle handoff — 2026-08-20, run 1

Scope of this run: BL-14, per the delivery driver's commission. One decision
written: **OD-13** (`docs/DESIGN.oracle.md`), superseding OD-5/R1001 by the
owner's 2026-08-19 ruling (`docs/DECISIONS.md`) and the amended R5/R28 in
`docs/DESIGN.md`, and adding **R1008** (per-path projection reporting, plus
bundles-per-whole-video, so the uncapped routing stays observable at the
checkpoint).

## Needs planning

- **Re-cut of the whole-transcript routing plan.** The steward writes a new
  plan under `docs/plans/oracle/` citing **OD-13**, covering **R28 and R1008**.
  What changes against the superseded plan:
  - Two routing paths, not three: `whole` and `excerpts`. The `EXCERPTS_OVER_CAP`
    path, the cap check in `choose_submission`, and the
    no-whole-block-over-the-cap invariant are gone with R1001.
  - A transcript larger than one bundle's token cap is delivered across
    **sequential bundles** rather than falling back to excerpts; the mechanics
    (splitting, ordering, how the extraction agent is told a block continues)
    are the plan's to specify, per amended R28.
  - Excerpt merging is transitive **clustering** per amended R5, computed on
    R1000's re-cut characters. The 80% ratio keeps its meaning.
  - Survives from the superseded plan: the bundle-XML `form` attribute, the
    per-path projection counts/characters/tokens (now under R1008), the
    characters-both-sides-joined-identically rule, and the
    exactly-at-the-threshold-goes-whole rule.

## Existing plans this touches

- `docs/plans/oracle/capped-whole-transcript-path.md` — **superseded, must not
  be built as merged.** All three slices implement the reversed cap.
- `docs/plans/oracle/recut-merged-excerpts.md` (OD-4/R1000) — unaffected; the
  owner's ruling explicitly keeps OD-4. Sequencing unchanged: it still lands
  before the re-cut routing plan, because the ratio is only honest once each
  transcript character is counted once.
- `docs/plans/whole-transcript-threshold.md` — still superseded and
  not-to-build; `CODEOWNERS` holds `docs/plans/`, so its revision (mark slice 1
  superseded, drop R28 from `covers:`) remains the owner's, as the superseded
  plan's Summary already asked.

## Do NOT act on yet

- **Do not build `capped-whole-transcript-path`** on the strength of its merged
  status. Its plan document stays in the tree (plans are not deleted by the
  oracle), but OD-13 supersedes its content.
- **Do not build `whole-transcript-threshold` slice 1** — unchanged standing
  instruction; it specifies the routing with no clustering and no
  sequential-bundle delivery.
- **Coverage note, not an action:** R1008 is uncovered until the steward's
  re-cut lands. That is the normal oracle→steward flow, not a gap to route
  around.

## Contradictions found and not decided

None in scope. BL-14 was this run's whole commission and is resolved by OD-13.

## Vision feedback

Nothing ambiguous this run. V4 and the durable-evidence section carried the
decision cleanly; V13 was the honest counter-statement and was answerable from
amended R5's clustering. One small observation for the owner: R28's amended
text now carries what is effectively an oracle-facing instruction ("this
supersedes R1001") inside a requirement. It worked here, but a requirement
that names ledger mechanics will read oddly once OD-13 has done the
superseding — future amendments might leave the mechanics to `docs/DECISIONS.md`
alone.
