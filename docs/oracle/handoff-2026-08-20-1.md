# Oracle handoff — 2026-08-20, run 1

Scope for this run: BL-14. One decision written: **OD-13**, superseding
OD-5/R1001 per the owner's 2026-08-19 ruling (`docs/DECISIONS.md`) and the
owner-amended `docs/DESIGN.md` R5/R28.

## Needs planning

- **Re-cut the whole-transcript routing plan to the clustering design.** The
  steward turns OD-13 into a new plan under `docs/plans/oracle/` (citing
  OD-13, covering R28), replacing `docs/plans/oracle/capped-whole-transcript-path.md`
  as the plan that gets built. OD-13 lists what the re-cut keeps and what it
  reverses. Two things the new plan must specify because R28 delegates them to
  it: the mechanics of delivering a transcript larger than one bundle's token
  cap across sequential bundles, and the projection counts that make the
  uncapped path observable at the checkpoint (videos sent whole; of those, how
  many spanned more than one bundle, and how many bundles each took).

## Existing plans each decision touches

- `docs/plans/oracle/capped-whole-transcript-path.md` — merged, partly wrong
  (cap check, `EXCERPTS_OVER_CAP`, `videos_over_cap`, the slice-2
  no-whole-block-over-cap invariant, the one-video-one-bundle premise).
  Verified unbuilt: no `submission.py` in the tree, no reference to
  `choose_submission` or R1001 in `src/` or `tests/`. It is a landed document
  and stays; the re-cut plan replaces it as the thing to build.
- `docs/plans/whole-transcript-threshold.md` — owner-owned; still says its
  slice 1 is blocked pending a ruling and still specifies an uncapped path
  *without* clustering. Its revision remains the owner's edit, as OD-5's plan
  already asked. Nothing new for the orchestrator here.
- `docs/plans/oracle/recut-merged-excerpts.md` (OD-4/R1000) — unaffected. The
  owner's ruling explicitly keeps OD-4, and the build-first sequencing stands:
  R1000's re-cut lands before the routing plan, because the R28 ratio is only
  honest once each transcript character is counted once.

## Do NOT act on yet

- **Do not build `capped-whole-transcript-path.md` as merged.** Its cap
  implements a decision the owner reversed; that is the HIGH risk BL-14 names.
- **Do not build `whole-transcript-threshold.md` slice 1.** Same routing, no
  clustering, and the plan's own text still forbids it.
- Build nothing from the re-cut until it lands: plans derive from the design
  layer and land before code (`AGENTS.md`).
