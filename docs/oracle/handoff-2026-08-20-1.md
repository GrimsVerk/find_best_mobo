# Oracle handoff — 2026-08-20, run 1

Scope for this run: BL-14, and only BL-14. One decision was appended:
**OD-13**, which supersedes OD-5/R1001 per the owner's 2026-08-19 ruling
(`docs/DECISIONS.md`) and the amended `docs/DESIGN.md` (R5/R28), and adds
**R1008** (the projection's per-path and multi-bundle reporting).

## Needs planning

- **A steward plan implementing OD-13.** Re-cut
  `docs/plans/oracle/capped-whole-transcript-path.md` into a new plan under
  `docs/plans/oracle/` that cites OD-13 and covers R1008 (delivering the
  amended R28 behaviour with it: uncapped whole-transcript path, sequential
  bundles for a transcript larger than one bundle's token cap, mechanics the
  plan's to specify). What survives from the old plan: `submission.py` as the
  routing home, the `form` field and bundle XML attribute, the ratio measured
  at-or-above 80% on merged-and-capped excerpts against
  `transcript_characters`, and per-path projection counts. What must not
  survive: the bundle-cap routing bound, the `EXCERPTS_OVER_CAP` path, and the
  no-whole-block-over-the-cap packer invariant.

## Existing plans each decision touches

- `docs/plans/oracle/capped-whole-transcript-path.md` — partly wrong;
  **do not build it as merged**. OD-13 names the surviving and the overruled
  parts.
- `docs/plans/oracle/recut-merged-excerpts.md` — untouched and confirmed
  (the owner's ruling states OD-4 stands). Its sequencing rule carries over:
  the re-cut merge lands before the routing plan builds, because the 80% ratio
  is only honest once each transcript character is counted once.
- `docs/plans/whole-transcript-threshold.md` — still must not be built, and
  now for a second reason: its slice 1 has no cap ruling *and* predates the
  clustering amendment. Its revision sits behind `CODEOWNERS` and is the
  owner's; nothing in this run changes that.

## Do not act on yet

- **Building any whole-transcript routing before the re-cut plan lands.**
  Two plans in the tree describe that routing and both are now partly wrong;
  the steward's re-cut is the only version that may be built.
- **BL-1, BL-2, BL-13's residue, and every other open backlog item** — outside
  this run's scope by the commissioner's instruction; not ruled, not touched.
  BL-13 itself is substantially answered by the owner's ruling and the amended
  design; anything left of it should be re-filed as its own item if it still
  contradicts anything.
