# Oracle handoff — 2026-08-20, run 8

Scope commissioned: BL-23. Decided; one decision appended to
`docs/DESIGN.oracle.md` (OD-22), one requirement added (R1011), no halts, no
waivers. Written once, per the rule — a correction gets the next-numbered file.

## What needs planning

- **OD-22 → one steward plan** under `docs/plans/oracle/` (suggested slug:
  `stage-b-extraction`). Covers R1011 together with R8, R9, R10, R26 and R27;
  elaborates the continue command R7 promises (one batch at a time). Boundary
  is BL-23's, not milestone M2's: it ends at a calibration batch extracted,
  validated, stored, its factor corrected, its record committed, and
  `acceptance/S3.sh` landed beside that first record. The minimal report
  generator (R16, R14's partial) is explicitly out; so are Stages C, D and E.
- **OD-14's R1009 is still unplanned** — pre-existing, not from this run. A
  steward dispatch for OD-14 is genuine and should produce the zero-duration
  warning plan.

After this run cites BL-23, step 3 stops firing and step 4's gap list maps to
OD-5 (false — OD-21 governs: write no plan, cite it, stop), OD-14 (genuine),
and OD-22 (genuine, this run's).

## Existing plans each decision touches

- **No merged plan needs a re-cut.** The Stage B plan is new work.
- **Sequencing the plan must carry:** extraction calibrates only against
  bundles from the amended Stage A — `recut-merged-excerpts` (R1000) and
  `capped-whole-transcript-path` (R28/R1008) are merged plans not yet built,
  and they build first. A factor calibrated against inflated excerpts is wrong
  by construction (BL-10's 4.8x).
- **`whole-transcript-threshold`**: OD-19's standing prohibition on its slice 1
  is unchanged.

## What the orchestrator should NOT act on yet

- **`PHASE=STEWARD ODS=OD-5`** — still the false dispatch; OD-21 is the whole
  job (cite it, no plan, stop).
- **M3+ milestone planning (R11–R16).** Once the owner's `coverage.sh` fix
  lands and `PHASE=PLAN` becomes reachable, those gaps surface. Stages C, D
  and E have no approved backlog item behind them, and BL-23's own approval
  logic — the first spending item needed its own approval — implies each later
  spending stage arrives with its own. Prefer surfacing that to the owner over
  planning them unattended; this is a note, not a ruling, and the owner can
  overrule it by filing the items.
- **The unmerged owner branch `docs/r26-fifteen-percent`** (R26: 15%, not 10%,
  plus its `docs/DECISIONS.md` entry). Owner-authored, owner-landed; nothing
  waits on it — R1011 reads the figure from R26 whenever it lands, and the
  Stage B implementation holds the fraction in one configured place so the
  edit reaches the guard as one line.

## For the owner (surface in the report; act on nothing)

- **The `coverage.sh` supersession fix is still the unlock for `PHASE=PLAN`**
  (OD-20/OD-21's priority list is unchanged). OD-22 does not touch that: it
  routes Stage B through the steward path, which is the designed route for
  oracle-added requirements — but milestone planning for anything else stays
  blocked until your one-line-class script edit lands.
- **`docs/r26-fifteen-percent` is yours to land.** Its own commit message says
  BL-23's "10%" must be read from R26; R1011 makes the figure single-sourced
  so the backlog's stale number can never reach code.
- **BL-1 and BL-2 stay deferred** — still outside every commissioned scope;
  OD-12 already sketched the plan revision they ride in.

## Ledger state after this run

Every ESC through ESC-23 and every BL through BL-23 is now either ruled
(OD-1..OD-22) or explicitly deferred with its reason (BL-1, BL-2).
`docs/acceptance.md` has no failing-criterion filings; no waiver was needed —
S3 is expected to genuinely pass once the calibration batch has run and its
record is committed.

## For the owner's report — vision notes

One framing note rather than an ambiguity: the vision's durable-evidence
section is written for unattended operation, while Stage B's batches are
deliberately owner-entered (§3: "Not intended to run unattended"). OD-22 read
the section as applying to any evidence future evaluation needs — the
calibration delta and the spend readings are exactly the "performance of the
changes downstream of the oracle's ruling" the owner's ruling names — not only
to evidence from unattended sessions. If the owner means it narrower, the
sentence to sharpen is that section's third bullet; R1011 would then rest on
S3's "mechanically checkable" promise alone, and nothing downstream changes.
