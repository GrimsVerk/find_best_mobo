# Oracle handoff — 2026-08-20, run 7

Scope commissioned: BL-22. Decided; one decision appended to
`docs/DESIGN.oracle.md` (OD-21), no new requirements, no halts, no waivers.
Written once, per the rule — a correction gets the next-numbered file.

## What needs planning

**Nothing.** OD-21 adds no requirement, so there is nothing for a steward to
cut. The livelock's fixes all live in owner-owned gate paths
(`.github/scripts/coverage.sh`, `.claude/scripts/deliver-phase.sh`,
`.github/scripts/oracle-decisions.sh`); what an oracle-writable path could do —
make the recurring false dispatch harmless and cheap, in design-layer text every
steward and the review gate reads — is done.

## What the driver and its stewards must observe from now on

- **`PHASE=STEWARD ODS=OD-5` is a false dispatch.** After this run cites BL-22,
  the driver's step 3 stops firing on it and step 4 resumes emitting that phase
  on every cycle — expected, and not an instruction that overrides the design
  layer. The dispatched steward writes no plan, reports OD-21, and stops. It
  does not re-derive the supersession from the ledger; the citation is the
  whole job. The rule is general: it applies to any `OD-<n>` whose every added
  requirement a later decision superseded.
- **OD-20's reading stands beneath it:** a `coverage.sh` exit 1 whose only gaps
  are superseded ids means "everything is planned". No plan names R1001 (or any
  superseded id) in `covers:` — that over-claim is forbidden by name.
- **Until the owner edits a script, the loop cannot advance past step 4.** Each
  cycle will be a cheap, recorded no-op steward. That is the designed state,
  not a malfunction to route around.

## For the owner (surface in the report; act on nothing) — priority raised

- **The `coverage.sh` supersession fix is now the unlock for all unattended
  progress**, not hygiene: `deliver-phase.sh`'s steward dispatch reads that
  script's gap list and nothing else, so the one edit OD-20 already specifies
  (subtract column-anchored `**Requirements superseded:**` ids; report them as
  their own excused class) closes BL-21 and BL-22 together. Owner-owned; the
  hole is the template's, so it belongs upstream.
- **Worth taking in the same motion:** the gate-side backstop BL-22 proposes —
  `oracle-decisions.sh` fails a plan under `docs/plans/oracle/` whose cited
  decisions' added requirements are all superseded — so a steward that misses a
  supersession is stopped by a check rather than by its own reading.
- **Fixing it re-arms BL-20.** The unstuck loop's plan walk will reach
  `whole-transcript-threshold` and dispatch its superseded slice 1; OD-19's
  standing prohibition governs that moment until the owner's closing edit on
  that plan lands (any of BL-20's three: `status: merged` after
  `feat/capped-whole-transcript-path` merges, delete the plan, or cut slice 1
  and R28 from it).

## Do NOT act on

- Editing `.github/scripts/coverage.sh`, `.claude/scripts/deliver-phase.sh`,
  `.github/scripts/oracle-decisions.sh`, or
  `docs/plans/whole-transcript-threshold.md` — all owner-owned; OD-1, OD-19,
  OD-20 and OD-21 all say so and route the fixes to the owner and the template.
- **BL-1 and BL-2** — still the only unruled backlog items, still outside the
  commissioned scope. Hand them to the next oracle cycle.

## Ledger state after this run

Every ESC through ESC-23 and every BL through BL-22 is now either ruled
(OD-1..OD-21) or explicitly deferred with its reason (BL-1, BL-2).
`docs/acceptance.md` has no failing-criterion filings; no waiver was needed.

## For the owner's report

Nothing ambiguous in `docs/VISION.md` this run. One note: this is the first
decision to lean on V3 for *process* cost — sessions burned on a livelocked
loop — rather than pipeline token spend. The sentence carried the weight
cleanly, but if the owner considers agent-session budget outside what V3 means
by "budget I could have used on other projects", that is the statement to
sharpen; OD-21 would then rest on the no-vision class with the same
alternatives, and nothing downstream would change.
