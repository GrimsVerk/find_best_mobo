# Oracle handoff — 2026-08-20, run 3

Scope of this run: BL-16 only, commissioned unattended by the delivery driver.

## Decided

- **OD-15** resolves **BL-16** (with BL-8 as its parent evidence): R1002's
  "measured 52-variant set" clause is satisfied by the steward's declared
  reconstruction — at least 52 variants across BL-8's damage classes, the three
  named failures verbatim, and a provenance note in the fixture itself. The
  reconstruction is the reference set from its landing. No requirement added or
  superseded; this is a clarifying decision on R1002, the same shape as OD-14.

## What needs planning

Nothing new. `docs/plans/oracle/caption-split-aliases.md` proceeded on exactly
the default OD-15 ratifies, so the plan needs **no re-cut** — it is buildable
as drafted. Its slice 3 keeps the full 52-variant fixture; the plan's offered
shrink to a failures-only fixture is declined by OD-15's alternatives.

## Not decided, and why

- The plan's second request — merge order between the OD-6 plan
  (`caption-split-aliases`), the OD-7 plan (same matcher), and the OD-11 plan
  (same table file) — is sequencing, not design. No `BL-<n>` logs it, so it is
  not this ledger's to rule on. For the orchestrator: the plans themselves
  already state the constraint (sequential, whichever lands second carries the
  rebase); schedule them one after another rather than in parallel.

## Nothing to hold back

No decision from this run should wait. OD-15 unblocks nothing that was red and
changes no built behaviour; it removes the LOW-risk uncertainty the plan
carried, so the plan may build whenever the orchestrator schedules it.

## Vision notes

- BL-8's measurement was taken offline and never committed, which is exactly
  the failure the durable-evidence section describes — but that section speaks
  of runs and review payloads, not of offline measurements an agent takes while
  drafting backlog evidence. A sentence saying that a measurement cited as
  evidence must land with the entry that cites it would have prevented BL-16
  from existing at all.
- No numbered V-statement addresses fixture or test provenance; OD-15 had to
  use the opt-out class and lean on V11's spirit by analogy. That was
  workable, and it is the second run in a row (after OD-10 through OD-12)
  where below-the-vision's-altitude process questions had no statement to
  cite. That is probably fine — the vision governs the product — but it is
  worth the owner knowing the opt-out class is carrying real weight.
