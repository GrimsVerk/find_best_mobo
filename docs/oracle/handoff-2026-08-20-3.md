# Oracle handoff — 2026-08-20, run 3

Scope commissioned: BL-17. Decided; one decision appended to
`docs/DESIGN.oracle.md` (OD-16, no new requirement). No halts, no waivers.
Written once, per the rule — a correction gets the next-numbered file.

## What needs planning

**Nothing.** OD-16 ratifies the LOW default the merged plan
`docs/plans/oracle/caption-split-aliases.md` proceeded on: the 52-variant
fixture lands as a labelled reconstruction, and that plan's slice 3 already
satisfies every constraint the decision states — the reconstruction note, the
three BL-8-named failures verbatim, the `steel-legend` case, the `never_match`
reject set, and the size floor with its provenance in the docstring. The plan
builds as merged; no re-cut, no covers change.

One constraint the builder of slice 3 must honour, because it is the only
place OD-16 tightens the plan's text rather than restating it: **a
failing-today `†` mark is demonstrated, or it is labelled unverified.** The
draft's five marks are recorded as failures only after being observed red
against the pre-fix matcher — which the plan already instructs the fixture
author to do; OD-16 makes it binding rather than advisory.

## Do NOT act on

- **The number 52 as a claim.** OD-16 records BL-8's original list as
  unrecoverable. No fixture, test docstring, or report may assert the
  reconstruction *is* the measured set; the count sizes the fixture and
  proves nothing. A future agent finding the real list somewhere would be
  logged evidence for superseding OD-16 — file it, don't quietly swap it in.
- **Cross-cue matching.** Unchanged from handoff-2026-08-20-2.md: OD-15's
  counter observes the boundary, the matcher does not cross it.
- **Sequencing unchanged.** The OD-13 re-cut of `capped-whole-transcript-path`
  and the OD-4/R1000 `recut-merged-excerpts` build are still queued ahead,
  one pipeline pull request in flight at a time; ordering among them and
  `caption-split-aliases` is the orchestrator's. The merged plan's own note
  stands: OD-7 (R1003) edits the same matcher and OD-11 (R1007) moves the
  same table file, so whichever lands second carries the rebase.
- **BL-1 and BL-2** — still the only unruled backlog items, still outside the
  commissioned scope. Hand them to the next oracle cycle.

## Ledger state after this run

Every ESC through ESC-23 and every BL through BL-17 is now either ruled
(OD-1..OD-16) or explicitly deferred with its reason (BL-1, BL-2).
`docs/acceptance.md` has no failing-criterion filings; no waiver was needed.

## For the owner's report

BL-17 existed because a measurement was taken and its data was never
committed — BL-8 recorded the count and the failures, not the list, so the
fixture the design later asked for could only be rebuilt. The vision's
durable-evidence section covers runs and reviews; it says nothing about
offline measurements an agent takes while investigating, which is where this
one evaporated. One sentence there — that a measurement cited as evidence
commits its data alongside its conclusions — would have made BL-17
unnecessary, and would prevent the next one.
