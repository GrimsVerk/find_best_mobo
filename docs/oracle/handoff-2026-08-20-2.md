# Oracle handoff — 2026-08-20, run 2

Scope for this run: BL-15. One decision written: **OD-14**, ruling that
OD-6/R1002's caption-split matching is per-cue — a split straddling two cues
stays out of scope, and the boundary is pinned as a negative test.

## Needs planning

- **Re-cut the OD-6 plan.** BL-15 was the ruling `docs/oracle/od-6-plan-draft.md`
  was waiting on; it is now answered. The steward writes a fresh plan under
  `docs/plans/oracle/`, citing OD-6 (and OD-14 for the scope), covering R1002,
  using the preserved draft as raw material, not as authority. Two things
  OD-14 requires of it beyond the draft: state cross-cue splits as *decided*
  out of scope (citing OD-14), and pin the boundary as a negative fixture
  case — a transcript where `toma` ends one cue and `hawk` starts the next
  yields no mention.
- The draft's other three open questions (the reconstructed 52-variant set,
  the table path, `airus master`) were all LOW-risk and were never filed as
  `BL-<n>` items. They stay the plan's to proceed on with recorded defaults;
  nothing here rules on them.

## Existing plans each decision touches

- `docs/oracle/od-6-plan-draft.md` — reference only, deliberately outside the
  scanned plan paths. OD-14 endorses its proposed default on cross-cue splits;
  its slice shapes survive unchanged, since the ruling keeps `find_mentions`'s
  signature and cue-at-a-time scan.
- `docs/plans/oracle/recut-merged-excerpts.md` (OD-4/R1000) and the OD-13
  re-cut of the routing plan — unaffected; OD-14 changes no signature they
  touch.
- Merge-order note the draft already carried, still true: OD-7/R1003 lands in
  the same matcher and OD-11/R1007 moves the same table file. Whichever plan
  lands second carries the rebase; that is for the plans to state.

## Do NOT act on yet

- Build nothing from the re-cut plan until it lands — plans derive from the
  design layer and land before code (`AGENTS.md`).
- Do not treat the preserved draft as a plan; its path is outside the plan
  scan on purpose.
