# Oracle handoff — 2026-08-20, run 5

Scope commissioned: BL-19. Decided; one decision appended to
`docs/DESIGN.oracle.md` (OD-18, no new requirement). No halts, no waivers.
Written once, per the rule — a correction gets the next-numbered file.

## What needs planning

**Nothing.** OD-18 ratifies the LOW default the merged plan
`docs/plans/oracle/subcommand-flag-forwarding.md` proceeded on: `--help`
following a subcommand is forwarded, so `find-best-mobo aliases --help` prints
the `aliases` parser's help — the only place `--check` is documented — while
`find-best-mobo --help` and a bare `find-best-mobo` keep today's output and
exit codes. The plan builds as merged; no re-cut, no covers change, and the
plan's own tests plus its slice-2 guard test (every command module answers
`--help`) are the measurement, so nothing new is commissioned.

## Do NOT act on

- **Building `subcommand-flag-forwarding` in parallel with
  `refuse-on-missing-artifact`.** The two plans edit the same four command
  modules; both plans already state they are never built in parallel, and
  whichever lands second carries the rebase. Sequencing among the queued
  builds stays the orchestrator's, one pipeline pull request in flight at a
  time.
- **Any new flag, a subcommand listing in the top-level help, or argparse
  subparsers.** The plan rules each out by name; OD-18 changes none of that.
- **BL-1 and BL-2** — still the only unruled backlog items, still outside the
  commissioned scope. Hand them to the next oracle cycle.

## Ledger state after this run

Every ESC through ESC-23 and every BL through BL-19 is now either ruled
(OD-1..OD-18) or explicitly deferred with its reason (BL-1, BL-2).
`docs/acceptance.md` has no failing-criterion filings; no waiver was needed.

## For the owner's report

Nothing new to wish from `docs/VISION.md` this run. OD-18 is the third ruling
in this family (after OD-10 and OD-12) to use the no-vision class for CLI and
process mechanics — that is the class doing its intended job, not a gap: the
vision governs the product's answers, and which help screen a dispatcher
prints is below its altitude. The alternatives field carries the weighing, as
the schema requires of that class.
