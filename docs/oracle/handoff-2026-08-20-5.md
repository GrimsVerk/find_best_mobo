# Oracle handoff — 2026-08-20, run 5

Scope of this run: BL-18 only, commissioned unattended by the delivery driver.

## Decided

- **OD-17** resolves **BL-18** (with BL-7 as its parent evidence): R1005 binds
  `aliases --check`. The rule's purpose — absence and emptiness are different
  facts and must read differently — applies wherever an upstream artifact is
  read, so whether the command is called a "diagnostic" (`scripts/run.sh`) or
  "the inspection stage" (`docs/architecture.md`) decides nothing. An absent
  transcript cache directory refuses naming `fetch`; a present-but-empty one
  reports every canonical as never matched, which is a true report of an empty
  corpus. No requirement added or superseded; this is a clarifying decision on
  R1005's scope, the same shape as OD-14 was for R1002.

## What needs planning

Nothing new. `docs/plans/oracle/refuse-on-missing-artifact.md` proceeded on
exactly the default OD-17 ratifies, so the plan needs **no re-cut** — it is
buildable as drafted. Its slice 3 already specifies both halves of the ruling
and the tests that pin them.

## Not decided, and why

Nothing else was in scope, and nothing adjacent forced itself in. The
terminology drift the evidence surfaced — `scripts/run.sh` and
`docs/plans/run-scripts.md` call `aliases` a diagnostic outside the four-stage
pipeline, `docs/architecture.md` calls it "the inspection stage" — is made
harmless by OD-17 (the term no longer selects behaviour), no `BL-<n>` logs it
as a defect, and aligning the wording is an ordinary edit for whichever change
next touches those documents, not a design ruling.

## Nothing to hold back

No decision from this run should wait. OD-17 unblocks nothing that was red and
removes the plan's only filed uncertainty, so the plan may build whenever the
orchestrator schedules it — sequenced against the other oracle plans exactly
as handoffs 2026-08-20-3 and -4 already said.

## Vision notes

- The opt-out class carried this ruling too. That is now five decisions
  (OD-10, OD-11, OD-12, OD-14, OD-17, besides the fixture pair OD-15/OD-16)
  where the question sat below the vision's altitude — process, placement, and
  scope questions about mechanisms rather than the product. Handoff
  2026-08-20-4 already flagged this pattern; it continues. If the owner wants
  these steerable, one sentence about what a tool must do when its input is
  missing versus empty would have decided BL-7 and BL-18 both — but the
  alternatives fields are carrying the weight adequately, so this is
  information, not a request.
- V12's "Silence has to read as silence" was the nearest statement and almost
  decided this: the principle (an empty result must read as empty, never as an
  error, and never be conjured from absence) is exactly what OD-9 and OD-17
  enforce at the artifact level. It is scoped to safety verdicts in the
  answer, so it was not relied on. If the owner ever widens that sentence
  beyond the report, this whole family of rulings acquires a citable anchor.
