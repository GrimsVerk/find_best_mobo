# Oracle handoff — 2026-08-20, run 4

Scope of this run: BL-17 only, commissioned unattended by the delivery driver.

## Decided

- **OD-16** resolves **BL-17** (with BL-9 as its parent evidence): R1003's
  "real B850I review's title" clause is satisfied by the plan's declared
  reconstruction — a title carrying the board's full name from BL-9
  (`MSI MPG B850I Edge TI review`), a docstring declaring the reconstruction
  and citing BL-9 and BL-17, and an assertion on the canonical `B850`
  admission, never on the string. No requirement added or superseded; this is
  a clarifying decision on R1003, the same shape as OD-15 was for R1002.

## What needs planning

Nothing new. `docs/plans/oracle/itx-chipset-variant.md` proceeded on exactly
the default OD-16 ratifies, so the plan needs **no re-cut** — it is buildable
as drafted. Its slice 3 already specifies the docstring provenance and the
canonical-only assertion OD-16 requires.

## Not decided, and why

- The plan's second request — merge order between `caption-split-aliases`
  (OD-6, same matcher function) and `itx-chipset-variant` (OD-7), with OD-11's
  table move behind both — is sequencing, not design, and no `BL-<n>` logs it.
  Handoff 2026-08-20-3 already said this for the same question asked from the
  other plan: schedule the three sequentially, whichever lands second carries
  the rebase, exactly as both plans themselves state.

## Nothing to hold back

No decision from this run should wait. OD-16 unblocks nothing that was red and
changes no built behaviour; it removes the last uncertainty the plan carried,
so the plan may build whenever the orchestrator schedules it.

## Vision notes

- BL-17 is the second consecutive backlog item (after BL-16) that exists only
  because a measurement was taken offline and its raw material — there the
  variant list, here the video's id and title — never landed with the entry
  citing it. The durable-evidence section speaks of runs and review payloads,
  not of evidence an agent gathers while drafting a backlog entry. One
  sentence — a measurement cited as evidence lands with the entry that cites
  it, ids and all — would have prevented both items, and would have given
  R1003's regression the real title instead of a reconstruction.
- No numbered V-statement addresses fixture provenance, so OD-16, like OD-15,
  used the opt-out class and leaned on V11's spirit by analogy. Workable, but
  the opt-out class is now carrying a recurring category (process and fixture
  questions below the vision's altitude), and the owner should know that.
