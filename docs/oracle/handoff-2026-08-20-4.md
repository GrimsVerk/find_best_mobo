# Oracle handoff — 2026-08-20, run 4

Scope commissioned: BL-18. Decided; one decision appended to
`docs/DESIGN.oracle.md` (OD-17, no new requirement). No halts, no waivers.
Written once, per the rule — a correction gets the next-numbered file.

## What needs planning

**Nothing.** OD-17 ratifies the LOW default the landed plan
`docs/plans/oracle/itx-chipset-variant.md` proceeded on: the R1003 regression's
title is a labelled reconstruction carrying BL-9's board name
(`MSI MPG B850I Edge TI`) verbatim, with the docstring declaring the measured
title absent from the record and nothing asserting on the invented wording.
The plan builds as landed; no re-cut, no covers change. This also answers the
plan's open question 1 (keep the surrounding title — the invented words buy
the mid-title boundary position and no assertion reads them) and question 2
(no derived-form marker in `aliases --check`; the plan's out-of-scope ruling
stands as written).

Two constraints OD-17 makes binding on slice 2's fixture author, because they
are where the plan's text is silent and the natural move is wrong:

- **The invented title padding must not contain any form the pre-fix matcher
  resolves to `B850`.** A bare `b850` token in the padding makes the
  regression green before the fix, and the blind test's red-then-green
  observation is the demonstration.
- **The reconstructed title is vendor-visible under the shipped table.**
  `msi` is a vendor form in `data/aliases.toml`, so a title carrying the board
  name verbatim title-hits MSI before and after the fix. The reconstruction
  reproduces exactly one measured property — BL-9's zero `B850` matches — and
  may not claim the real video's recorded title-invisibility (BL-9, OD-8).
  The selection test's "excluded before this plan" half is therefore only
  demonstrable against the test's own `tmp_path` table, with no `msi`-like
  form reachable from the title it uses; reusing the reconstructed title
  against a table that carries vendors makes that claim false pre-fix.

## Do NOT act on

- **Fetching the real title from YouTube.** OD-17 rejected it: a title fetched
  today is not the title BL-9 measured, and the offline suite cannot repeat
  the fetch. An agent that does recover the *measured* title string somewhere
  has logged evidence for superseding OD-17 — file it, never swap it in
  silently.
- **Merge order unchanged** (the plan's open question 3): the landed note
  stands — `caption-split-aliases` edits the same `compile_matcher` and
  OD-11/R1007 moves the same table file, so `itx-chipset-variant` must not be
  built in parallel with either, and whichever lands second carries the
  rebase. Sequencing among the queued builds (OD-13's re-cut, OD-4/R1000)
  remains the orchestrator's, one pipeline pull request in flight at a time.
- **BL-1 and BL-2** — still the only unruled backlog items, still outside the
  commissioned scope. Hand them to the next oracle cycle.

## Ledger state after this run

Every ESC through ESC-23 and every BL through BL-18 is now either ruled
(OD-1..OD-17) or explicitly deferred with its reason (BL-1, BL-2).
`docs/acceptance.md` has no failing-criterion filings; no waiver was needed.

## For the owner's report

BL-18 is BL-17's shape again, one run later: a measurement was taken, its
conclusion was recorded, and the string it was taken against was not —
`data/index.jsonl` is gitignored, so the title BL-9 measured against left the
tree with the container. The previous handoff already named the vision gap:
the durable-evidence section covers runs and reviews, not the data an agent
measures against while investigating. One sentence there — a measurement cited
as evidence commits its inputs alongside its conclusions — would have made
both BL-17 and BL-18 unnecessary. Two filings in two runs is the log pointing
at the same missing check twice.
